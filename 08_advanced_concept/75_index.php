if there is an index.html fime in a folder, php display the file instead of opening the folder up in the browser
if there is an index.php and index.html file, the browser picks up the index.html file.
index.php gets picked up and opens when index.html is absent